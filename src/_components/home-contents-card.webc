<a webc:root="override" class="home-contents-card">
  <slot></slot>
  <svg
    class="strokes"
    aria-hidden="true"
    xmlns="http://www.w3.org/2000/svg"
  ></svg>
</a>

<style webc:scoped>
  :host {
    background-color: var(--color-accent-primary);
    color: var(--color-offwhite);
    display: grid;
    overflow: hidden;
    position: relative;
  }

  .title,
  picture {
    grid-area: 1 / 1;
  }

  .title {
    display: block;
    height: auto;
    max-width: 83%;
    place-self: center;
    z-index: 3;

    &[alt] {
      color: var(--color-offwhite);
      font-size: 2rem;
    }
  }

  :host(:hover) .title,
  :host(:focus-within) .title {
    scale: 1.05;
  }

  picture {
    mix-blend-mode: luminosity;
    position: relative;
  }

  .strokes {
    display: block;
    height: 100%;
    inset: 0;
    opacity: 0;
    pointer-events: none;
    position: absolute;
    width: 100%;
    z-index: 2;
  }

  :host(:hover) .strokes,
  :host(:focus-within) .strokes {
    opacity: 1;
  }

  .graphic {
    display: block;
    height: 100%;
    object-fit: cover;
    object-position: center;
    transition: scale 2s ease;
    width: 100%;
  }

  :host(:hover) .graphic,
  :host(:focus-within) .graphic {
    scale: 1.05;
  }
</style>

<style>
  html[style*="color-scheme: dark"] {
    .home-contents-card picture {
      filter: contrast(0.75);
      mix-blend-mode: darken;
    }
  }
</style>

<script webc:bucket="components">
  initDrawStrokes(".home-contents-card", {
    filterId: "card-warp",
    yBias: "edges",
  });
</script>
