<ul>
  <li webc:for="spec of specs">
    <p class="label" @text="spec.title"></p>
    <p @html="spec.desc"></p>
  </li>
</ul>

<style webc:scoped>
  :host {
    display: block;
    max-width: var(--g-max-6);
    padding-top: calc(var(--g-row) * 6);
  }

  ul {
    display: flex;
    flex-wrap: wrap;
    column-gap: 1rem;
    row-gap: 1.5rem;
  }

  li {
    width: 100%;
  }

  .label {
    font-family: var(--font-accent);
    font-size: var(--font-size-12);
    font-weight: 600;
    margin-bottom: 0.25rem;
    text-transform: uppercase;
  }

  p {
    font-size: var(--font-size-18);
    line-height: 1.33;
  }

  @media (min-width: 620px) {
    li {
      width: calc(33% - 1rem);
    }
  }
</style>
